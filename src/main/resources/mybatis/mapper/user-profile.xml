<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.letsparty.mapper.MyMapper">
	
	<insert id="addProfile" parameterType="com.letsparty.vo.UserProfile">
		insert into user_profile
		(user_id, profile_nickname, image_filename, is_default)
		value
		(#{id}, #{nickname}, #{filename}, #{isDefault})
	</insert>
	
</mapper>